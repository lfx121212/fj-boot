package ${package.Service};

import ${package.EntityVo}.${entityVo};
import ${superServiceClassPackage};

/**
 * <p>
 * $!{table.comment} 服务类
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
#if(${kotlin})
interface ${table.serviceName} : ${superServiceClass}<${entityVo}>
#else
public interface ${table.serviceName} extends ${superServiceClass}<${entityVo}> {

}
#end
